sudo: required
services:
  - docker

before_install:
  - docker build -t davidfosterwalrus/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true davidfosterwalrus/docker-react npm run test
